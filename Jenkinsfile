pipeline {
         agent any
         stages {
                 stage('One') {
                 steps {
                     echo 'Hi, this is Zulaikha from devops'
                 }
                 }
                 stage('two') {
                 steps { 
                     echo "Running the unit test..."
                           }
                           }
         }
}
