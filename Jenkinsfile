pipeline {
         agent any
         stages {
                 stage('One') {
                 steps {
                     echo 'Hi, this is vibhakar'
                 }
                 }
                 stage('two') {
                 steps { 
                     echo "Running the unit test..."
                           }
                           }
         }
}
